services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: spont
      POSTGRES_PASSWORD: spont
      POSTGRES_DB: spont
    ports:
      - "5432:5432"
    volumes:
      - spont_pg_data:/var/lib/postgresql/data

volumes:
  spont_pg_data:
